// core — общая библиотека: Image, Kernel, свёртка, IO, тесты-свойства.
// Используется всеми модулями task1, task2, task3.
// java-library плагин даёт конфигурацию `api` для проброса транзитивных зависимостей.
plugins {
    `java-library`
}
